import CookiePreferences from './CookiePreferences';

export const metadata = { title: 'Cookie Preferences — Starbucks' };

export default function CookiePreferencesPage() {
  return <CookiePreferences />;
}
